'use client'
import type { JourneyRef } from '../use-journey'

/** Task 86 placeholder — filled by the DesertGroves dressing pass. */
export function DesertGroves({ journeyRef }: { journeyRef: JourneyRef }) {
  void journeyRef
  return null
}
