'use client'
import type { JourneyRef } from '../use-journey'

/** Task 86 placeholder — filled by the CanyonBadlands dressing pass. */
export function CanyonBadlands({ journeyRef }: { journeyRef: JourneyRef }) {
  void journeyRef
  return null
}
