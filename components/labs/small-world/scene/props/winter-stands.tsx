'use client'
import type { JourneyRef } from '../use-journey'

/** Task 86 placeholder — filled by the WinterStands dressing pass. */
export function WinterStands({ journeyRef }: { journeyRef: JourneyRef }) {
  void journeyRef
  return null
}
