'use client'
import type { JourneyRef } from '../use-journey'

/** Task 86 placeholder — filled by the DeltaShoals dressing pass. */
export function DeltaShoals({ journeyRef }: { journeyRef: JourneyRef }) {
  void journeyRef
  return null
}
