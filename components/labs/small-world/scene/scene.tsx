import type { MutableRefObject } from 'react'

/** Stub — Task 6 replaces this with the real three.js scene. */
export function SmallWorldScene(_: { progressRef: MutableRefObject<number> }) {
  return null
}
