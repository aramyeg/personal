/**
 * The character GLB and the idle clip baked into it. Named for its ROLE, never
 * its identity — the asset is drop-in swappable and nothing here assumes one
 * (character-agnostic law: no gendered names anywhere near it). This is the
 * single canonical definition of the source path + clip name for the lab.
 */
export const CHARACTER_SRC = '/labs/memory-card/models/character.glb'
export const CHARACTER_IDLE_CLIP = 'Armature.F|bashful'
